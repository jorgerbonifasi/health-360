# Frontend (Vite) — copy to web/.env.local. Only VITE_-prefixed vars are exposed to the browser.
# The anon key is safe to ship to the client; RLS restricts it to SELECT-only on data tables.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
